import React from 'react';
import Header from '../../components/header/Header';
import Footer from '../../components/footer/Footer';
// Add any necessary CSS imports

const Login = () => {
  // Copy the main content from login.tsx here, removing Next.js-specific code
  // and using only React best practices.
  return (
    <>
      <Header onSearch={() => {}} search={false} />
      <div className="admin-login-container">
        {/* ...login page content... */}
        {/* You will need to copy the JSX from the original login.tsx here. */}
      </div>
      <Footer />
    </>
  );
};

export default Login; 